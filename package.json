{
  "name": "voice-guided-interview-server",
  "version": "0.0.0",
  "description": "Server for voice-guided-interview",
  "main": "app.js",
  "scripts": {
    "build": "babel src --out-dir lib",
    "start": "npm run build && node lib/app.js",
    "test": "npm run mocha",
    "mocha": "mocha test/routes --compilers js:babel-register",
    "mocha:watch": "mocha test/routes test/util --compilers js:babel-register --watch"
  },
  "author": "",
  "license": "MIT",
  "dependencies": {
    "body-parser": "^1.15.0",
    "debug": "^2.2.0",
    "feathers": "^2.0.0",
    "feathers-rest": "^1.2.3",
    "natural-brain": "^0.2.0"
  },
  "devDependencies": {
    "babel-cli": "^6.6.4",
    "babel-preset-es2015": "^6.6.0"
  }
}
